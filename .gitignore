# --- Rust ------------------------------------------------------------------
/target/
**/*.rs.bk

# Cargo.lock IS committed — this crate is a binary, not a library.

# --- not for publication ----------------------------------------------------
# Keyrock's own hiring material; the repo is public.
docs/
# Raw briefs are working input; the spec packet (spec/plan/tasks) is the artefact.
specs/*/inputs/

# --- secrets ---------------------------------------------------------------
.env
.env.*
!.env.example

# --- local working material -------------------------------------------------
# specs/ is deliberately NOT here — spec packets are tracked, unlike the rest.
.claude/
CLAUDE.md
NOTES.md
scratch/

# --- OS / editor -----------------------------------------------------------
.DS_Store
.idea/
.vscode/
*.swp
