# Without this, every `docker build` uploads the whole build tree as context.
target/

.git/
.gitignore
.env
.env.*
!.env.example

# Agent scaffolding and working notes — not part of the deliverable, and not
# something the image needs.
.claude/
CLAUDE.md
specs/
docs/

.DS_Store
Dockerfile
compose.yml
.dockerignore
README.md
